language: python

python: "3.6.5"

install:
  - pip install -r requirements.txt
  - pip install .
# command to run tests
script:
  - pytest

after_success:
  - "BRANCHES_TO_MERGE_REGEX='^d/' BRANCH_TO_MERGE_INTO=master GITHUB_REPO=ying-wen/malib ./travis-automerge"